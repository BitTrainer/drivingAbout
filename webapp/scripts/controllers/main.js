'use strict';

/**
 * @ngdoc function
 * @name drivingAboutApp.controller:MainCtrl
 * @description
 * # MainCtrl
 * Controller of the drivingAboutApp
 */
angular.module('drivingAboutApp')
  .controller('MainCtrl',[ '$scope',  function (scope) {
    
  }]);
